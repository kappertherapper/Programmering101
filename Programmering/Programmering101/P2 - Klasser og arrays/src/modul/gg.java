package modul;

public class gg {
    //int count = 0;
    //for (int i = 0; i < arraywithnull.length; i++) {
       // if (arraywithnull [i] != null) {
            //newarray[count] = arraywithnull[i]; count++;
        }

        //public static int maxGrade(int[] arr, int size) {
               // int max = Integer.MIN_VALUE;
                //int returnableIndex = -1;
                // loop to find highest grade in array
                //for (int i = 0; i < size; i++) {
                        //if (arr[i] > max) {
                               // max = arr[i];
                               // returnableIndex = i;
                       // }
               // }
               // return returnableIndex;
     //   }
